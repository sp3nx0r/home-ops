# Enable Talos 1.14 workload isolation (sandboxd) — runs CRI/kubelet/pods in a
# dedicated PID/mount namespace. New 1.14 clusters default to this; upgraded
# clusters like ours were opted out and must enable it explicitly.
---
apiVersion: v1alpha1
kind: SecurityProfileConfig
workloadIsolation: true
