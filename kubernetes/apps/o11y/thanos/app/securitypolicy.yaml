---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/gateway.envoyproxy.io/securitypolicy_v1alpha1.json
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: thanos-oidc
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: thanos-query
  oidc:
    provider:
      issuer: "https://id.${SECRET_DOMAIN}"
    clientID: "REPLACE_WITH_THANOS_POCKET_ID_CLIENT_ID"
    clientSecret:
      name: thanos-oidc-secret
    redirectURL: "https://thanos.${SECRET_DOMAIN}/oauth2/callback"
    logoutPath: "/logout"
    cookieDomain: "${SECRET_DOMAIN}"
    cookieNames:
      idToken: thanos-id-token
      accessToken: thanos-access-token
    forwardAccessToken: true
    scopes:
      - openid
      - profile
      - email
      - groups
