---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./secret.sops.yaml
configMapGenerator:
  - name: grafana-dashboard-backblaze-b2-capacity
    files:
      - ./dashboards/backblaze-b2-capacity.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Backups
  - name: grafana-dashboard-ollama
    files:
      - ./dashboards/ollama.json
    options:
      labels:
        grafana_dashboard: "1"
      annotations:
        grafana_folder: Ollama
generatorOptions:
  disableNameSuffixHash: true
